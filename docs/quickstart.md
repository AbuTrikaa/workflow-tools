# Quickstart

1. Run `./scripts/setup_workspace.sh` to create a workspace.
2. Edit `examples/sample_config.yml` if desired.
3. Run:
   ```bash
   python3 tools/report_generator.py examples/sample_config.yml
